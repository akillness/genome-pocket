# Least Recently Used Caching

Least recently used cache eviction. An LRU cache removes the least recently used
item once capacity is reached. Recency of access decides which entry is evicted
as the victim entry during eviction.
