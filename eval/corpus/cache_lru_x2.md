# LRU Victim Selection

Selecting an eviction victim by recency. An LRU cache evicts the least recently
used entry when full, removing the least recently used victim entry. Eviction is
driven by access recency in the cache.
