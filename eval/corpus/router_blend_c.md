# More Validation

Teams rely on validation for consistency. The validation step reduces surprises.
Documenting validation keeps a service coherent, so validation is written down.
Validation, validation, validation across the codebase.
